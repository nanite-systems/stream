import { REQUEST } from '@nestjs/core';

export const BASE_STREAM = 'provider:base_stream';
export const SUBSCRIPTION_STREAM = 'provider:subscription_stream';
export const CENSUS_STREAM = 'provide:census_stream';

export const WS_REQUEST = REQUEST;
